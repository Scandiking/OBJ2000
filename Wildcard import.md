The wildcard import imports all the classes in a package. Use when multiple classes from the same package are needed. Not recommended in large projects due to potential conflicts.

`import java.util.*;`

